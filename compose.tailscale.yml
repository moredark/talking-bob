services:
  admin:
    build:
      context: ./admin
    restart: unless-stopped
    environment:
      PORT: ${PORT:-3000}
    ports:
      - "127.0.0.1:${ADMIN_PORT:-8080}:8080"
    depends_on:
      app:
        condition: service_healthy

