## 1. Принятие baseline

- [x] 1.1 Согласовать с пользователем `specs/report-delivery/spec.md` и границы переноса из proposal; подтверждение должно относиться к подготовленному тексту, а не только к началу работы над этапом.

## 2. Перенос источника истины

- [x] 2.1 Перенести принятые требования в `openspec/specs/report-delivery/spec.md` через sync-specs; проверить совпадение всех семи требований и сценариев с принятой baseline и корректный Purpose.
- [x] 2.2 Используя подготовленный `documentation.patch` как проверяемый diff, в той же правке заменить в `docs/app.md` два предложения от `A generated report is persisted` до `resend the saved result`, абзац от `Reports are formatted as literal plain text` до `same persisted report` и последнее предложение о `/report` в Privacy and retention ссылками на основную spec; проверить, что readiness, generation/retries, квоты, стрик и сроки retention сохранены.
- [x] 2.3 Обновить `docs/architecture/README.md` отдельной строкой про нормативный контракт доставки и исключением из общей строки application behavior; уточнить приоритет перенесённых specs в Source of truth файла `AGENTS.md`, добавить соответствующий маршрут в `docs/sdd-workflow.md` и убрать устаревшую фразу о пустом реестре specs/changes. Проверить, что одна область имеет один нормативный источник, а правило GitHub не изменилось.

## 3. Проверки и завершение

- [x] 3.1 Сверить финальный текст с картой в `verification.md`; использовать уже выполненные `npm run build` и четыре report/journey test-файла, если проверенный код не менялся. Если обнаружен пробел, добавить только соответствующий тест и повторить затронутые проверки под Node.js 24.18.0; записать фактический результат и ограничения.
- [x] 3.2 Выполнить `openspec validate --all --strict --no-interactive`, проверить локальные ссылки и `git diff --check`; подтвердить, что валидатор проверил и change, и основную spec, а runtime/schema/dependencies не менялись.
- [x] 3.3 Провести review финального переноса и записать evidence в `verification.md`; подтвердить принятие пользователем, завершение переноса и успешные проверки перед отметкой этапа выполненным.

`design.md` осознанно пропущен по условию `openspec instructions design`: нет
изменений архитектуры, данных, зависимостей или алгоритма. Статус CLI может
показывать отсутствующий design; это не незавершённый технический дизайн.

Baseline и перенос согласованы пользователем 2026-09-12; review завершён. Открытых продуктовых вопросов нет.

После выполнения всех пунктов архивировать change. Перед использованием
`documentation.patch` повторить `git apply --check`; при изменившемся контексте
актуализировать diff, сохранив согласованные границы.
