## 1. Совместимое хранение расписания и действия сообщения

- [x] 1.1 Добавить в `prisma/schema.prisma` и новую additive migration `User.promptWeekdaysMask` с default 127 и SQL CHECK 1–127, а также nullable `Broadcast.messageAction` с CHECK поддерживаемого действия; числовые фильтры размещаются в существующем filters JSON. Проверить Prisma validation и migration fixtures: старые enabled/disabled users, время, стрики и queued broadcasts сохраняются, у старых рассылок action=null и прежние filters; невалидные маски/действия отвергаются PostgreSQL.
- [x] 1.2 Обновить create/upsert в `UserService`, optional patch в `ScheduleSettings` и необходимые типы без переименования существующих полей/фасадных методов; проверить сценарии `Compatible schedule defaults` и сохранение маски при старых time/on/off вызовах в `test/user-prompt-settings-lifecycle.test.js` и `test/service-facade-contracts.test.js` после `npm run prisma:generate` и `npm run build`.

## 2. Календарное расписание и восстановление

- [x] 2.1 Добавить в `shared/time` вычисления выбранных локальных дней с прежней DST-политикой, не меняя daily helpers стриков; проверить в `test/timezone-schedule.test.js` время до/ровно/после слота, переход недели, UTC/local-date расхождение, gap/overlap и пропущенную целую календарную дату из `Local calendar scheduling`.
- [x] 2.2 Обновить update, startup normalization и repair в `schedule-settings.operations.ts`: сохранять mask/time/enabled/cursor атомарно, отключение даёт null, повторное включение восстанавливает выбранные дни; проверить `Weekly or manual practice schedule`, ошибки/откат, отсутствие отправок при normalization и исправление stale cursor на невыбранную будущую дату ещё до его наступления в `test/schedule-lifecycle.test.js`.
- [x] 2.3 Обновить `schedule-claims.operations.ts`: загружать маску, резервировать только допустимый сегодняшний due-slot, пропускать прошлые даты и продвигать курсор даже при пустом каталоге/конфликте occurrence; проверить оба сценария `Recovery respects selected dates` и changed daily downtime behavior в `test/schedule-lifecycle.test.js`.
- [x] 2.4 Проверить конкуренцию и совместимость на реальном PostgreSQL в `integration/postgres-critical-invariants.integration.js`: одновременные claims/settings, immutable occurrence после смены времени/дней, прежний reclaim и неизвестные результаты, отсутствие deadlock; отдельно подтвердить, что административный on/off сохраняет маску и нормальный cursor, при необходимости поправив существующую audited transaction без расширения API. Проверка — `npm run test:postgres`.

## 3. Telegram: расписание и произвольное время

- [x] 3.1 Добавить `/schedule`, `/time`, `/time ЧЧ:ММ`, `schedule_open` и меню команд в существующем TelegramModule; handlers делегируют сохранение ScheduleService. Проверить `Unified schedule entry points`, unknown user, открытие старой кнопки и отсутствие расхода dialog quota в `test/user-prompt-settings-lifecycle.test.js` и `test/user-journey.test.js`.
- [x] 3.2 Реализовать inline-черновик дней с подтверждением, «Каждый день», «Занимаюсь сам», сохранённый часовой пояс и следующий слот; проверить выбор Пн/Ср/Пт, отмену, пустую маску, повторный save и несколько открытых меню, где сохранение дней не откатывает более новое время, в ближайших settings tests.
- [x] 3.3 Реализовать валидацию произвольного времени и совместимые `set_time_*` callbacks; проверить 00:00/23:59/07:25, отклонение 24:00/12:60/8:5/лишних аргументов, сохранение disabled/mask, ошибки хранилища и инструкцию без аргумента по `Arbitrary question time command`. Убедиться runtime-тестом, что обычный текст и voice не стали вводом времени.
- [x] 3.4 Добавить в `/schedule` кнопку открытия picker времени, в `/time` — пресеты 09:00/13:00/18:00 и инструкцию отправить своё время в чат. Принимать ровно `ЧЧ:ММ` только в личном чате от того же sender в течение 10 минут; закрывать контекст после успеха, slash-команды или callback вне меню времени; ошибки допускают повтор до истечения срока; не перехватывать voice всегда и обычный текст вне окна и не сохранять контекст в БД. Проверить success/invalid/expiry/foreign-chat и сохранение дней/enabled в Telegram tests.

## 4. Независимый выбор напоминаний о стрике

- [x] 4.1 После сохранения неполной недели и ручного режима показывать русский текст ежедневного правила и абсолютные действия включения/выключения reminders с текущим состоянием; использовать существующий `StreakService.updateReminderEnabled`. Проверить `Explain daily qualification for a sparse schedule` и `Explicit independent reminder choice`: отсутствие выбора, повтор disable, ошибка отдельного сохранения и возврат к ежедневному расписанию в settings tests.
- [x] 4.2 Добавить регрессионные сценарии `Preserve the existing streak contract` в `test/streak-lifecycle.test.js` и `test/streak-reminder-dispatcher.test.js`: пропуск невыбранного дня рвёт серию, ручное занятие продолжает её, включённый reminder возможен в свободный день; подтвердить сохранение qualification/expiry/longest/report snapshots существующими assertions без изменения алгоритма стрика.

## 5. Гибкие настройки рассылки в существующем backend

- [x] 5.1 Расширить broadcast contracts/validation/normalization и административные ответы двумя независимыми optional числовыми фильтрами `noVoiceForDays`/`scheduledDeliveryWithinDays`, nullable `messageAction` и моментом preview. Проверить в `test/admin-broadcasts.test.js` прежние валидные запросы и пять routes, нейтральные omission/null, границы 1/365, отклонение 0/отрицательных/дробей/366/строк/boolean, неизвестных действий и полей; list/detail/audit сохраняют настроенные параметры без подстановок.
- [x] 5.2 Реализовать независимые условия Prisma preview/recheck и SQL snapshot через durable last voice и EXISTS scheduled delivery, соединяя их с прежними фильтрами через AND. Проверить `Independent audience filters` и `Precise activity and delivery windows` при фиксированном T: оба фильтра выключены, каждый отдельно, N=14/M=14 и N=30/M=7, противоречивое пересечение, null/before/exact/after voice cutoff, sentAt на обеих границах, несколько deliveries без дублей, manual/streak-only/failed/pending/ambiguous sends и независимый enabled-фильтр. Добавить parity реальных recipient IDs на PostgreSQL в `integration/admin-mvp.integration.js` и проверить план запроса.
- [x] 5.3 В `AdminBroadcastsService` фиксировать один T при create для audience snapshot/createdAt, атомарно сохранять введённый текст, выбранные фильтры, optional action и аудит через прежний INSERT SELECT. Проверить preview/create drift, аудиторию, ставшую пустой между ними, сохранение разных N/M и отсутствие загрузки истории в память в `test/admin-broadcasts.test.js` и PostgreSQL integration.
- [x] 5.4 Передавать сохранённое действие в совместимый sender options и Telegram adapter. При хотя бы одном новом активном периоде перед каждой attempt повторять все настроенные условия со свежим T; без периодов сохранять прежнюю pre-send проверку даже с кнопкой. Проверить `Free message text and optional action`, `Recheck configured audience without growing the snapshot` и `Preserve delivery safety and configuration`: action+no filters, filters+no action, old rows, reply/opt-out/disable/ban/status/delivery aging, retry-safe/ambiguous/restart/concurrency и отказ отправки при повреждённой новой конфигурации. Проверка — `test/admin-broadcasts.test.js`, включая аргументы Telegram API adapter.

## 6. Админка: фильтры, сообщение и подтверждение

- [x] 6.1 Обновить `admin/src/types/index.ts`, `admin.api.ts` и `BroadcastCreatePage.vue`: отдельно включаемые периоды N/M, существующие AND-фильтры, собственный текст и выбор «Без кнопки»/«Настроить расписание». Добавить параметры в signature, используя установленные shadcn-vue компоненты. Проверить начальное состояние (пустой текст, новые фильтры выключены, кнопки нет), отсутствие шаблонов/пресетов/выбора вида, независимость полей и preview текста, действия, всех фильтров, evaluatedAt и числа адресатов.
- [x] 6.2 Сохранить актуальность preview, обработку опоздавших ответов, empty/error/retry/disabled состояния и подтверждение; актуализировать `admin/test/broadcasts.contract.test.mjs` и пройти локальный UI-поток с разными N/M и включением/отключением действия без реальной отправки. Проверка — `npm run test:admin` и записанный ручной просмотр, включая клавиатурную навигацию, ошибки ввода и запрет подтверждения при неактуальном preview.
- [x] 6.3 Отобразить сохранённые фильтры и действие в `BroadcastsPage.vue` и `BroadcastDetailPage.vue`, уточнить подписи «Вопросы по расписанию», сохранить пагинацию/cancel/purged состояния. Проверить old/new broadcasts, отсутствие периодов/кнопки у legacy и показ настроек при очищенном теле без восстановления текста через admin contract tests и `npm run test:admin`.

## 7. Интеграция, review и нормативные документы

- [x] 7.1 Пройти целевой end-to-end сценарий в тестах: администратор вручную задаёт N=14/M=14/enabled=true, вводит свой вопрос об удобстве времени и выбирает open_schedule → preview/create → eligible inactive user получает текст с кнопкой → `/schedule` → Пн/Ср/Пт → `/time 08:45` → явный выбор reminders → ручной урок в свободный день. Расширить `test/user-journey.test.js`, без готовой конфигурации и реальных отправок; проверка — `npm run test:smoke`.
- [x] 7.2 Выполнить итоговые gates `npm run test:ci`, `npm run test:postgres`, `npm run test:admin`; зафиксировать фактические команды, результаты и ограничения в `verification.md` этого change. Устранить связанные с изменением ошибки и повторить только необходимые проверки; пропущенный gate не считать успешным.
- [x] 7.3 Выполнить read-only review реализации и миграции: границы модулей, schedule/streak separation, eligibility parity, claims/lock order, совместимость старых API и UI accessibility. Проверка завершения — рассмотренные замечания с исправлениями либо обоснованным решением и отсутствие незакрытых существенных дефектов в `verification.md`.
- [x] 7.4 После подтверждения реализации синхронизировать три delta specs в основной каталог; в той же правке заменить только нормативные дубли перенесённого расписания в `docs/app.md` ссылкой, сохранив полный streak contract. Обновить `docs/database.md`, карту источников истины и затронутые `docs/architecture/03-telegram-conversation.md`, `04-background-jobs.md`, `06-admin-and-operations.md`; проверить согласованность spec/schema/SQL и существование ссылок.
- [x] 7.5 Обновить эксплуатационные указания по порядку migration → backend preview check → admin, read-only rollback SQL и предпочтению forward fix. Проверить gates на enabled/disabled неполных масках и queued/processing рассылках с каждым новым фильтром отдельно, обоими фильтрами, только действием и legacy-конфигурацией; старый runtime не должен игнорировать новые параметры. Описать ручной подтверждаемый запуск без реальной отправки. Сверить документ с Migration Plan и выполнить `openspec validate --all --strict --no-interactive` и `git diff --check`. Отметить tasks только по фактическому результату; реализация не означает отправку рассылки или автоматическое архивирование change.
